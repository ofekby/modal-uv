[project]
name = "modal-uv-playbook-mnist-cnn"
version = "0.1.0"
description = "Train a small MNIST CNN on Modal with modal-uv"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.7,<3",
    "torchvision>=0.22,<1",
]

[tool.uv]
package = false
